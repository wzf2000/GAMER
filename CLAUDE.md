@AGENTS.md

## Claude Code

- Treat `AGENTS.md` as the shared source of project instructions for this repository.
- Add Claude-specific guidance here only when it should not apply to Codex or other agents.
- Keep personal, machine-local preferences in `CLAUDE.local.md` and add that file to `.gitignore` if created.
- For larger Claude-only path-specific guidance, use `.claude/rules/*.md` with `paths` frontmatter instead of growing this file.
